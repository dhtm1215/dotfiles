# ~/.config/zsh/40-starship.zsh

# Powerlevel10k is the active prompt. Loading Starship as well registers a
# second prompt hook and causes both prompts to render.
# has starship && eval "$(starship init zsh)"
